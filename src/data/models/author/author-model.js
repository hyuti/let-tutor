class AuthorModel {
    constructor(id, name, avatar, tutorsCount) {
        this.id = id
        this.name = name
        this.avatar = avatar
        this.tutorsCount = tutorsCount
    }
}

export default AuthorModel